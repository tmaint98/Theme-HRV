StreetVibe.Footer = {
		init: function() {
			var that = this;
      that.StreetVibe_toggleFooter();
      that.StreetVibe_newsletterForm();
		},
  StreetVibe_toggleFooter: function(){
		$('.streetvibe-title-footer').on('click', function(){
			if ($(window).width() < 992) {
				jQuery(this).toggleClass('opened').parent().find('.streetvibe-block-collapse').stop().slideToggle('medium');
			}
		});
	},
  StreetVibe_newsletterForm: function(){
		if($('.streetvibe-newsletter-form').length > 0){
			$('.streetvibe-newsletter-form form.contact-form').submit(function(e) { 
				var self = $(this);
				if($(this)[0].checkValidity() == true){
					e.preventDefault();
					grecaptcha.ready(function() {
						grecaptcha.execute('6LchSLkqAAAAABVHBpeFgg8N-WgkYsr5fO6GUF_s', {action: 'submit'}).then(function(token) {
							self.find('input[name="g-recaptcha-response"]').val(token);
							$.ajax({
								type: 'POST',
								url:'/account/contact',
								data: $('.streetvibe-newsletter-form form.contact-form').serialize(),
								success:function(data){		
									if($(data).find('#streetvibe-newsletter-success').length > 0){
										$('.streetvibe-newsletter-form .streetvibe-newsletter-error').html('');
										swal({
											icon: "success",
											className: "streetvibe-newsletter-form-success",
											title: "Đăng kí thông tin thành công",
											text: "Thông báo sẽ tự động tắt sau 5 giây...",
											button: false,
											timer: 5000,
										});
										e.target.reset();
									} else {
										$('.streetvibe-newsletter-form .streetvibe-newsletter-error').html('Địa chỉ email không hợp lệ');
									}
								},	
							})
						}); 
					});
				}
			});
		}
	},
};
(function() {
  StreetVibe.Footer.init();
})();
