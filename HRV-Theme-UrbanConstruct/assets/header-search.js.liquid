UrbanConstruct.Search = {
  init: function(){
    var that = this;
    that.UrbanConstruct_searchAutoHeader();
  },
  UrbanConstruct_searchAutoHeader: function(){
		$('.urbanconstruct-ultimate-search').submit(function(e) {
			e.preventDefault();
			var q = $(this).find('input[name=q]').val();
			if(q.indexOf('script') > -1 || q.indexOf('>') > -1){
				alert('Từ khóa của bạn có chứa mã độc hại! Vui lòng nhập lại từ khóa khác');
				$(this).find('input[name=q]').val('');
			}
			else{
				var q_follow = 'product';
				var query = encodeURIComponent('(title:product contains ' + q + ')');
				if( !q ) {
					window.location = '/search?type='+ q_follow +'&q=*';
					return;
				}	
				else {
					window.location = '/search?type=' + q_follow +'&q=filter=' + query;
					return;
				}
			}
		});
		var $input = $('.urbanconstruct-ultimate-search input[type="text"]');
		$input.bind('keyup change paste propertychange', function() {
			var key = $(this).val(),
					$parent = $(this).parents('.urbanconstruct-wpo-wrapper-search'),
					$results = $(this).parents('.urbanconstruct-wpo-wrapper-search').find('.urbanconstruct-smart-search-wrapper');

			if(key.indexOf('script') > -1 || key.indexOf('>') > -1){
				alert('Từ khóa của bạn có chứa mã độc hại! Vui lòng nhập lại từ khóa khác');
				$(this).val('');
				$('.urbanconstruct-ultimate-search input[type="text"]').val('');
			}
			else{
				if(key.length > 0 ){
					$(this).attr('data-history', key);
					$('.urbanconstruct-ultimate-search input[type="text"]').val($(this).val());
					var q_follow = 'product',
							str = '';
					str = '/search?type=product&q=' + key + '&view=header-ultimate-product';
					$.ajax({
						url: str,
						type: 'GET',
						async: true,
						success: function(data){
							$results.find('.urbanconstruct-resultsContent').html(data);
						}
					})
					if(!$('.urbanconstruct-header-action_search').hasClass('urbanconstruct-js-action-show')){
						$('body').removeClass("locked-scroll");
						$('.urbanconstruct-header-action-item').removeClass('urbanconstruct-js-action-show');
					}
					$("urbanconstruct-ultimate-search").addClass("expanded");
					$results.fadeIn();
				}
				else{
					$('urbanconstruct-ultimate-search input[type="text"]').val($(this).val());
					$("urbanconstruct-ultimate-search").removeClass("expanded");
					$results.fadeOut();
				}
			}

		})
		$('body').click(function(evt) {
			var target = evt.target;
			if (target.id !== 'urbanconstruct-ajaxSearchResults' && target.id !== 'urbanconstruct-inputSearchAuto') {
				$("#urbanconstruct-ajaxSearchResults").hide();
			}
			if (target.id !== 'urbanconstruct-ajaxSearchResults-mb' && target.id !== 'urbanconstruct-inputSearchAuto-mb') {
				$("#urbanconstruct-ajaxSearchResults-mb").hide();
			}
			if (target.id !== 'urbanconstruct-ajaxSearchResults-3' && target.id !== 'urbanconstruct-inputSearchAuto-3') {
				$("#urbanconstruct-ajaxSearchResults-3").hide();
			}
		});
		$('body').on('click', '.urbanconstruct-ultimate-search input[type="text"]', function() {
			if ($(this).is(":focus")) {
				if ($(this).val() != '') {
					$(".urbanconstruct-ajaxSearchResults").show();
				}
			} else {
			}
		})
		$('body').on('click', '.urbanconstruct-ultimate-search .search-close', function(e){
			e.preventDefault();
			$(".urbanconstruct-ajaxSearchResults").hide();
			$(".urbanconstruct-ultimate-search").removeClass("expanded");
			$(".urbanconstruct-ultimate-search").find('input[name=q]').val('');
		})
	},
};

(function() {
  UrbanConstruct.Search.init();
})();


