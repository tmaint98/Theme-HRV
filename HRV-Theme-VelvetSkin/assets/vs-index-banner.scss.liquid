.velvetskin-index-banner {
  .banner-item {
    position: relative;
    overflow: hidden;
    .title {
      position: absolute;
      // inset: 0;
      top: 0;
      left: 0;
      backdrop-filter: blur(8px);
      color: #fff;
      border: 1px solid #fff;
      text-transform: uppercase;
      border-radius: 50%;
      width: 150px;
      height: 150px;
      padding: 10px;
      margin: auto;
      display: flex;
      justify-content: center;
      align-items: center;
      transition: transform 0.08s ease
      , opacity 0.1s ease;
      will-change: transform
      , opacity;
      opacity: 0;
    }
  }
  @media(max-width: 1199px) {
    .banner-item {
      .title {
        opacity: 1;
        inset: 0;
      }
    }
  }
  @media(max-width: 767px) {
    .banner-item {
      .title {
        width: 120px;
        height: 120px;
      }
    }
  }
}