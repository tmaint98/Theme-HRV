
{%comment%}<link rel="preload" href="{{ 'dh-product-loop.scss.css' | asset_url }}" as="style">
<link rel="stylesheet" href="{{ 'dh-product-loop.scss.css' | asset_url }}">
<script fetchpriority="low" type="text/javascript" defer src="{{ 'dh-product-loop.js' | asset_url }}"></script>{%endcomment%}
{%- assign has_section_collection = false -%}
{%- assign has_section_collection_slide = false -%}
{%- assign has_heading = false -%}

{%- for i in (1..11) -%}
{%- capture sectionname -%}dentalhub_section_index_{{i}}{%- endcapture -%}
{%- if settings[sectionname] != 'none' -%}
{%- include settings[sectionname] -%}
{%- endif -%}
{%- endfor -%}