DentalHub.IndexAjax = {
		init: function() {
			var that = this;
      that.DentalHub_IndexAjax();
		},
  DentalHub_IndexAjax: function(){
    $('.dentalhub-index-ajax').each(function(){
      const $this = $(this);
      let id = $this.attr('id');
      let urls = '{{shop.url}}?view=' + id;
      let hd = $this.attr('data-handle');
      if($this.hasClass('index-col') || $this.hasClass('index-col-slide')){
        if($this.hasClass('index-col')){
          urls = '/collections/' + hd + '?view=col-data';
        }else{
          urls = '/collections/' + hd + '?view=slide-data';
        }
      }else if($this.hasClass('index-blog')){
        urls = '/blogs/' + hd + '?view=index-data';
      }
      setTimeout(function(){ 
        $.ajax({
          url: urls,					
          type: 'GET',
          async: false,
          success:function(data){
            setTimeout(function(){ 
              if($this.hasClass('index-review')){
                $('.dentalhub-index-ajax[id="'+ id +'"]').html(data);
                DentalHub.IndexReviewSlider.DentalHub_IndexReviewSlider();
              }else if($this.hasClass('index-col')){
                $('.dentalhub-index-col[data-handle="'+hd+'"] .list-pro').html(data);
                DentalHub.ProductLoop.DentalHub_loopImgSlider();
              }else if($this.hasClass('index-col-slide')){
                $('.dentalhub-index-col-slide[data-handle="'+hd+'"] .list-pro').html(data);
                DentalHub.IndexColSlider.DentalHub_IndexColSlider(hd);
                DentalHub.ProductLoop.DentalHub_loopImgSlider();
              }else if($this.hasClass('index-blog')){
                $('.dentalhub-index-blog .list-blog').html(data);
              }else{
                $('.dentalhub-index-ajax[id="'+ id +'"]').html(data);
              }
            }, 300);
          }
        });
      }, 500);
    })
    
  }
};
(function() {
  DentalHub.IndexAjax.init();
})();




