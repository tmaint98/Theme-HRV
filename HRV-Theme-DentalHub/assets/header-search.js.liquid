DentalHub.Search = {
  init: function(){
    var that = this;
    that.DentalHub_searchAutoHeader();
  },
  DentalHub_searchAutoHeader: function(){
		$('.dentalhub-ultimate-search').submit(function(e) {
			e.preventDefault();
			var q = $(this).find('input[name=q]').val();
			if(q.indexOf('script') > -1 || q.indexOf('>') > -1){
				alert('Từ khóa của bạn có chứa mã độc hại! Vui lòng nhập lại từ khóa khác');
				$(this).find('input[name=q]').val('');
			}
			else{
				var q_follow = 'product';
				var query = encodeURIComponent('(title:product contains ' + q + ')');
				if( !q ) {
					window.location = '/search?type='+ q_follow +'&q=*';
					return;
				}	
				else {
					window.location = '/search?type=' + q_follow +'&q=filter=' + query;
					return;
				}
			}
		});
		var $input = $('.dentalhub-ultimate-search input[type="text"]');
		$input.bind('keyup change paste propertychange', function() {
			var key = $(this).val(),
					$parent = $(this).parents('.dentalhub-wpo-wrapper-search'),
					$results = $(this).parents('.dentalhub-wpo-wrapper-search').find('.dentalhub-smart-search-wrapper');

			if(key.indexOf('script') > -1 || key.indexOf('>') > -1){
				alert('Từ khóa của bạn có chứa mã độc hại! Vui lòng nhập lại từ khóa khác');
				$(this).val('');
				$('.dentalhub-ultimate-search input[type="text"]').val('');
			}
			else{
				if(key.length > 0 ){
					$(this).attr('data-history', key);
					$('.dentalhub-ultimate-search input[type="text"]').val($(this).val());
					var q_follow = 'product',
							str = '';
					str = '/search?type=product&q=' + key + '&view=header-ultimate-product';
					$.ajax({
						url: str,
						type: 'GET',
						async: true,
						success: function(data){
							$results.find('.dentalhub-resultsContent').html(data);
						}
					})
					if(!$('.dentalhub-header-action_search').hasClass('dentalhub-js-action-show')){
						$('body').removeClass("locked-scroll");
						$('.dentalhub-header-action-item').removeClass('dentalhub-js-action-show');
					}
					$("dentalhub-ultimate-search").addClass("expanded");
					$results.fadeIn();
				}
				else{
					$('dentalhub-ultimate-search input[type="text"]').val($(this).val());
					$("dentalhub-ultimate-search").removeClass("expanded");
					$results.fadeOut();
				}
			}

		})
		$('body').click(function(evt) {
			var target = evt.target;
			if (target.id !== 'dentalhub-ajaxSearchResults' && target.id !== 'dentalhub-inputSearchAuto') {
				$("#dentalhub-ajaxSearchResults").hide();
			}
			if (target.id !== 'dentalhub-ajaxSearchResults-mb' && target.id !== 'dentalhub-inputSearchAuto-mb') {
				$("#dentalhub-ajaxSearchResults-mb").hide();
			}
			if (target.id !== 'dentalhub-ajaxSearchResults-3' && target.id !== 'dentalhub-inputSearchAuto-3') {
				$("#dentalhub-ajaxSearchResults-3").hide();
			}
		});
		$('body').on('click', '.dentalhub-ultimate-search input[type="text"]', function() {
			if ($(this).is(":focus")) {
				if ($(this).val() != '') {
					$(".dentalhub-ajaxSearchResults").show();
				}
			} else {
			}
		})
		$('body').on('click', '.dentalhub-ultimate-search .search-close', function(e){
			e.preventDefault();
			$(".dentalhub-ajaxSearchResults").hide();
			$(".dentalhub-ultimate-search").removeClass("expanded");
			$(".dentalhub-ultimate-search").find('input[name=q]').val('');
		})
	},
};

(function() {
  DentalHub.Search.init();
})();

