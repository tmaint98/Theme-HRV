BambooCraft.Search = {
  init: function(){
    var that = this;
    that.BambooCraft_searchAutoHeader();
  },
  BambooCraft_searchAutoHeader: function(){
		$('.bamboocraft-ultimate-search').submit(function(e) {
			e.preventDefault();
			var q = $(this).find('input[name=q]').val();
			if(q.indexOf('script') > -1 || q.indexOf('>') > -1){
				alert('Từ khóa của bạn có chứa mã độc hại! Vui lòng nhập lại từ khóa khác');
				$(this).find('input[name=q]').val('');
			}
			else{
				var q_follow = 'product';
				var query = encodeURIComponent('(title:product contains ' + q + ')');
				if( !q ) {
					window.location = '/search?type='+ q_follow +'&q=*';
					return;
				}	
				else {
					window.location = '/search?type=' + q_follow +'&q=filter=' + query;
					return;
				}
			}
		});
		var $input = $('.bamboocraft-ultimate-search input[type="text"]');
		$input.bind('keyup change paste propertychange', function() {
			var key = $(this).val(),
					$parent = $(this).parents('.bamboocraft-wpo-wrapper-search'),
					$results = $(this).parents('.bamboocraft-wpo-wrapper-search').find('.bamboocraft-smart-search-wrapper');

			if(key.indexOf('script') > -1 || key.indexOf('>') > -1){
				alert('Từ khóa của bạn có chứa mã độc hại! Vui lòng nhập lại từ khóa khác');
				$(this).val('');
				$('.bamboocraft-ultimate-search input[type="text"]').val('');
			}
			else{
				if(key.length > 0 ){
					$(this).attr('data-history', key);
					$('.bamboocraft-ultimate-search input[type="text"]').val($(this).val());
					var q_follow = 'product',
							str = '';
					str = '/search?type=product&q=' + key + '&view=header-ultimate-product';
					$.ajax({
						url: str,
						type: 'GET',
						async: true,
						success: function(data){
							$results.find('.bamboocraft-resultsContent').html(data);
						}
					})
					if(!$('.bamboocraft-header-action_search').hasClass('bamboocraft-js-action-show')){
						$('body').removeClass("locked-scroll");
						$('.bamboocraft-header-action-item').removeClass('bamboocraft-js-action-show');
					}
					$("bamboocraft-ultimate-search").addClass("expanded");
					$results.fadeIn();
				}
				else{
					$('bamboocraft-ultimate-search input[type="text"]').val($(this).val());
					$("bamboocraft-ultimate-search").removeClass("expanded");
					$results.fadeOut();
				}
			}

		})
		$('body').click(function(evt) {
			var target = evt.target;
			if (target.id !== 'bamboocraft-ajaxSearchResults' && target.id !== 'bamboocraft-inputSearchAuto') {
				$("#bamboocraft-ajaxSearchResults").hide();
			}
			if (target.id !== 'bamboocraft-ajaxSearchResults-mb' && target.id !== 'bamboocraft-inputSearchAuto-mb') {
				$("#bamboocraft-ajaxSearchResults-mb").hide();
			}
			if (target.id !== 'bamboocraft-ajaxSearchResults-3' && target.id !== 'bamboocraft-inputSearchAuto-3') {
				$("#bamboocraft-ajaxSearchResults-3").hide();
			}
		});
		$('body').on('click', '.bamboocraft-ultimate-search input[type="text"]', function() {
			if ($(this).is(":focus")) {
				if ($(this).val() != '') {
					$(".bamboocraft-ajaxSearchResults").show();
				}
			} else {
			}
		})
		$('body').on('click', '.bamboocraft-ultimate-search .search-close', function(e){
			e.preventDefault();
			$(".bamboocraft-ajaxSearchResults").hide();
			$(".bamboocraft-ultimate-search").removeClass("expanded");
			$(".bamboocraft-ultimate-search").find('input[name=q]').val('');
		})
	},
};

(function() {
  BambooCraft.Search.init();
})();

