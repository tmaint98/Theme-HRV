RebelLook.Addthis = {
		init: function() {
			var that = this;
      that.RebelLook_addthisIconShare();
      that.RebelLook_clickIconsToolbar();
		},
  RebelLook_addthisIconShare: function(){
		if ($('.rebellook_addthis_listSharing').length > 0){
			$(window).scroll(function(){
				if($(window).scrollTop() > 250 ) {
					$('.rebellook_addthis_listSharing_list').addClass('is-show');
				} else {
					$('.rebellook_addthis_listSharing_list').removeClass('is-show');
				}
			});
			$('.rebellook_addthis_contact__icons .rebellook-box-contact,.rebellook_addthis_contact__lists .rebellook_addthis_close').on('click', function(e){
				if($('.rebellook_addthis_listSharing').hasClass('active')){
					$('body').removeClass('rebellook-locked-scroll');
					$('.rebellook_addthis_listSharing').removeClass('active');
					$('.rebellook_addthis_listSharing').fadeOut(150);				
				}
				else{		
					$('.rebellook_addthis_listSharing').fadeIn(100);
					$('.rebellook_addthis_listSharing').addClass('active');
				}
			});
			$("body").on('click', function(event) {
				if ($(event.target).is('.rebellook_addthis_contact__dialog') || $(event.target).is('.addThiclose')) {
					event.preventDefault();
					$('body').removeClass('rebellook-locked-scroll');
					$('.rebellook_addthis_listSharing').removeClass('active');
					$('.rebellook_addthis_listSharing').fadeOut(150);			
				}
			});
			$('.rebellook-body-popupform form.contact-form').submit(function(e){
				var self = $(this);
				if($(this)[0].checkValidity() == true){
					e.preventDefault();
					grecaptcha.ready(function() {
						grecaptcha.execute('6LchSLkqAAAAABVHBpeFgg8N-WgkYsr5fO6GUF_s', {action: 'submit'}).then(function(token) {
							self.find('input[name="g-recaptcha-response"]').val(token);
							$.ajax({
								type: 'POST',
								url:'/contact',
								data: $('.rebellook-body-popupform form.contact-form').serialize(),			 
								success:function(data){		
									$('.rebellook-modal-addthis.fade.show').modal('hide');
									swal({
										icon: "success",
										className: "rebellook-form-success",
										title: "{%if settings.rebellook_addthis_icon3_title2 != ''%}{{settings.rebellook_addthis_icon3_title2}}{%else%}Đăng kí thông tin thành công{%endif%}",
										text: "{{settings.rebellook_addthis_icon3_content}}",
										button: false,
										timer: 5000,
									}).then((result) => {
										//location.reload(); 
									});
								},	
							})
						}); 
					});
				}
			});
		}
		/*if ($('.layoutProduct_scroll').length > 0 ) {
			if	(jQuery(window).width() < 768 ){
				var curScrollTop = 0;
				$(window).scroll(function(){	
					var scrollTop = $(window).scrollTop();
					if(scrollTop > curScrollTop  && scrollTop > 200 ) {
						$('.layoutProduct_scroll').removeClass('scroll-down').addClass('scroll-up');
					}
					else {
						if (scrollTop > 200 && scrollTop + $(window).height() + 150 < $(document).height()) {
							$('.layoutProduct_scroll').removeClass('scroll-up').addClass('scroll-down');	
						}
					}
					if(scrollTop < curScrollTop  && scrollTop < 200 ) {
						$('.layoutProduct_scroll').removeClass('scroll-up').removeClass('scroll-down');
					}
					curScrollTop = scrollTop;
				});
			}
		}*/
	},
  RebelLook_clickIconsToolbar: function(){
		$('.rebellook-toolbarCartClick').click(function(){
			if($('.rebellook_addthis_listSharing').hasClass('active')){
				$('.rebellook_addthis_listSharing').removeClass('active');
				$('.rebellook_addthis_listSharing').fadeOut(150);	
			}
			$('body').addClass('rebellook-locked-scroll');
      $('.rebellook-header-action-item').removeClass('rebellook-js-action-show');
			$('.rebellook-sidebar-main').addClass('rebellook-is-show-right');
			$('.rebellook-sidebar-main .rebellook-sitenav-cart').addClass('rebellook-show');
		});
		$('.rebellook-toolbarContactClick').click(function(){
			if($('.rebellook_addthis_listSharing').hasClass('active')){
				$('body').removeClass('rebellook-locked-scroll');
				$('.rebellook_addthis_listSharing').removeClass('active');
				$('.rebellook_addthis_listSharing').fadeOut(150);				
			}
			else{		
				$('body').addClass('rebellook-locked-scroll');
				$('.rebellook_addthis_listSharing').fadeIn(100);
				$('.rebellook_addthis_listSharing').addClass('active');
			}
		});
		$('.rebellook-toolbarMenuClick').click(function(){
			$('.rebellook-activeMenuChecked').trigger('click');
		});
	},
};
(function() {
  RebelLook.Addthis.init();
})();


